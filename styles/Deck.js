import { StyleSheet } from 'react-native'

export default deckStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
    },
    headingText: {
        fontSize: 20,
        textAlign: 'center',
        color: 'midnightblue',
        fontWeight: '900',
    },
})
