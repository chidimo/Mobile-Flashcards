import { StyleSheet } from 'react-native'
import { purple } from '../utils/colors';

export default deckViewDetailsStyles = StyleSheet.create({

    cardCountText: {
        textAlign: 'center',
        color: purple
    },
})
