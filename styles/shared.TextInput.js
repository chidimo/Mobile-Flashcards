import { StyleSheet } from 'react-native'

import { loveRomance } from '../utils/colors';

export default sharedTextInputStyles = StyleSheet.create({
    textInput: {
        height: 40,
        borderColor: loveRomance,
        borderBottomWidth: StyleSheet.hairlineWidth,
        marginBottom: 20,
    },
})
