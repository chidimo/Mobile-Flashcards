import { StyleSheet } from "react-native";

export const sharedStyles = StyleSheet.create({
  headingText: {
    fontSize: 24,
    textAlign: "center",
    color: "purple",
    fontWeight: "900",
  },
});
